import { Center } from "@chakra-ui/react";

const SectioSubtitle = ({ children }) => {
  return (
    <Center id={"section_subtitles"} fontSize={"4xl"}>
      {children}
    </Center>
  );
};

export default SectioSubtitle;
